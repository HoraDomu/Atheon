name: skill-env-exfil-webhook
category: security-hardening
match: '(os\.environ|getenv|secrets\.manager)\s*.*\b(discord|slack|telegram|webhook)\b.*(curl|wget|post|send|http)'
enabled: true
severity: critical
description: "Skill appears to send environment credentials to external webhook"
reference: "https://llmtop10.com/"
tags:
  - skill-security
  - credential-exfiltration
